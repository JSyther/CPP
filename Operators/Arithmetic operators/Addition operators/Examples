1. Adding two integers:

int x = 5;
int y = 10;
int sum = x + y; // sum will be 15
-----------------------------------------------------------------------------------------------------------------------------------------------------------------------

2. Adding two floating-point numbers:

float x = 1.5;
float y = 3.5;
float sum = x + y; // sum will be 5.0
-----------------------------------------------------------------------------------------------------------------------------------------------------------------------

3. Adding two characters:

char x = 'a';
char y = 'b';
char sum = x + y; // sum will be 'c'
-----------------------------------------------------------------------------------------------------------------------------------------------------------------------

4. Adding two boolean values:

bool x = true;
bool y = false;
int sum = x + y; // sum will be 1 (true is represented as 1)
-----------------------------------------------------------------------------------------------------------------------------------------------------------------------

5. Adding a number to a variable:

int x = 5;
x = x + 1; // x will now be 6
-----------------------------------------------------------------------------------------------------------------------------------------------------------------------

6. Adding the contents of two variables and storing the result in a third variable:

int x = 5;
int y = 10;
int sum = x + y; // sum will be 15
-----------------------------------------------------------------------------------------------------------------------------------------------------------------------

7. Adding a constant to a variable:

int x = 5;
x = x + 10; // x will now be 15
-----------------------------------------------------------------------------------------------------------------------------------------------------------------------

8. Adding a value to an array element:

int myArray[5] = {1, 2, 3, 4, 5};
myArray[2] = myArray[2] + 2; // myArray[2] will now be 5
-----------------------------------------------------------------------------------------------------------------------------------------------------------------------

9. Adding two pointers:

int x = 5;
int* pX = &x;
int* pY = pX + 1; // pY will point to the memory address one unit larger than the memory address pointed to by pX
-----------------------------------------------------------------------------------------------------------------------------------------------------------------------

10. Adding the contents of two arrays and storing the result in a third array:

int arr1[5] = {1, 2, 3, 4, 5};
int arr2[5] = {6, 7, 8, 9, 10};
int sumArr[5];
for (int i = 0; i < 5; i++) {
    sumArr[i] = arr1[i] + arr2[i];
} // sumArr will now contain {7, 9, 11, 13, 15}
